<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.acorn.MycourseMapper"> 
    
    <select id="selectMyCourse" resultType="MycourseDTO" parameterType="String">
   	    SELECT * FROM mycourseTbl WHERE userid = #{userId} ORDER BY coursenum
	</select>
	
	<insert id="insertMyCourse" parameterType="Map" useGeneratedKeys="true" keyProperty="coursenum">
	    INSERT INTO mycourseTbl (userid, coursecode, coursename, coursenum,coursecontent, firstimage)
	    VALUES (#{userId}, 'C' || coursenum_seq.NEXTVAL, #{courseName}, coursenum_seq.NEXTVAL,'' ,'https://korean.visitkorea.or.kr/resources/images/common/no_img_01.png')
	</insert>
	
	<delete id="deleteMyCourse" parameterType="Map">
	    DELETE FROM mycourseTbl WHERE userid = #{userId} AND coursecode = #{coursecode}
	</delete>
	
	<select id="selectOneMyCourse" resultType="MycourseDTO" parameterType="Map">
	    SELECT userid, coursecode, coursename, coursecontent FROM mycourseTbl WHERE userid = #{userId} AND coursecode = #{coursecode}
	</select>
	
	<update id="updateMyCourse" parameterType="MycourseDTO">
	    UPDATE mycourseTbl SET coursename = #{coursename}, coursecontent = #{coursecontent} WHERE userid = #{userId} AND coursecode = #{coursecode}
	</update>
	
	<select id="selectMycourseList" resultType="MycourseDTO" parameterType="String">
	    SELECT m.coursecode, m.contentid, m.contentnum, c.title, c.address, c.firstimage, c.mapx, c.mapy, c.contenttypeid, c.cat2
	    FROM mycourseContentTbl m JOIN contentinfotbl c ON m.contentid = c.contentid WHERE m.coursecode = #{coursecode} ORDER BY contentnum
	</select>
	
	<select id="countInsertMycourseList" resultType="Integer" parameterType="Map">
	    SELECT COUNT(*) FROM mycourseContentTbl WHERE coursecode = #{coursecode} AND contentid = #{contentid}
	</select>
	
	<insert id="insertMyCourseList" parameterType="Map">
	    INSERT INTO mycourseContentTbl (coursecode, contentid, contentnum) VALUES (#{coursecode}, #{contentid}, #{contentnum})
	</insert>
	
	<delete id="deleteMyCourseList" parameterType="Map">
	    DELETE FROM mycourseContentTbl WHERE coursecode = #{coursecode} AND contentid = #{contentid}
	</delete>
 
</mapper>